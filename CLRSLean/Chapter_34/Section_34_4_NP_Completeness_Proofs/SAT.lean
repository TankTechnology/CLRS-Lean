import CLRSLean.Chapter_34.Section_34_4_NP_Completeness_Proofs.SAT.Verification

/-!
# Standalone SAT certificate semantics

This facade exposes the canonical raw assignment format, total checker, exact
all-input acceptance semantics, and linear certificate bound for {lit}`SAT`.
The fixed-machine realization is intentionally kept as the next separate
closure layer.
-/
