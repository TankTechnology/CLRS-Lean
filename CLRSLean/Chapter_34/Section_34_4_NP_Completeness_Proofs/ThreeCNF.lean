import CLRSLean.Chapter_34.Section_34_4_NP_Completeness_Proofs.ThreeCNF.Verification

/-!
# Standalone 3-CNF-SAT certificate semantics

This facade exposes the canonical raw assignment format, total checker, exact
all-input acceptance semantics, and linear certificate bound for
{lit}`ThreeCNFSat`.  The fixed-machine realization is intentionally kept as the
next separate closure layer.
-/
