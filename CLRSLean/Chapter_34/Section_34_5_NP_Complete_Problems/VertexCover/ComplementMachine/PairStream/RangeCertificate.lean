import CLRSLean.Chapter_34.Section_34_5_NP_Complete_Problems.VertexCover.ComplementMachine.PairStream.RangeCertificate.Runtime

/-!
# VERTEX-COVER pair stream: graph-to-range-certificate machine

A fixed polynomial-time TM2 extracts the unary vertex bound from a canonical
graph and emits the exact certificate `[0, ..., |V| - 1]` consumed by the
reused general-CLIQUE pair generator.
-/
