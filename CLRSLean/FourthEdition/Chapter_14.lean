import CLRSLean.Chapter_15

/-!
# Chapter 14 — Dynamic Programming

This is the canonical CLRS fourth-edition chapter guide during the migration
period.

## Current source

During the compatibility period this guide imports {lit}`CLRSLean.Chapter_15`. Existing declarations retain their current namespaces until the chapter-by-chapter source migration.

## Coverage boundary

The third-edition Chapter 15 development supplies proved example-level
optimality and pure-execution results, but it does not yet seal the fourth-edition
algorithm boundary.  Exact gaps are tracked for cut reconstruction/memoization,
tabulated matrix-chain and LCS algorithms, the generic Elements-of-DP interface,
and public executable OBST tables with their cost theorems.

See {lit}`docs/clrs-fourth-edition-map.csv` for the section-level mapping and
{lit}`docs/migrations/clrs4.md` for compatibility and deprecation policy.
-/
