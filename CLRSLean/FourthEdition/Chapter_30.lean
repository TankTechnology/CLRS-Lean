import CLRSLean.Chapter_30

/-!
# Chapter 30 — Polynomials and the FFT

This is the canonical CLRS fourth-edition chapter guide during the migration
period.

## Current source

During the compatibility period this guide imports {lit}`CLRSLean.Chapter_30`. Existing declarations retain their current namespaces until the chapter-by-chapter source migration.

## Coverage boundary

The DFT recursive FFT polynomial multiplication and FFT-circuit developments are reused; iterative FFT is separately cataloged as Online Material.

See {lit}`docs/clrs-fourth-edition-map.csv` for the section-level mapping and
{lit}`docs/migrations/clrs4.md` for compatibility and deprecation policy.
-/
