# Chapter 15 semantic-closure implementation plan

1. Add the textbook activity validity layer, iterative selector, equivalence
   theorem, and exact scan-count theorem; compile a focused test and commit.
2. Repair the §15.2 terminology, separate the two structural predicates,
   instantiate the generic framework for activity selection, test, and commit.
3. Add Huffman's equation (15.4) bridge, standalone textbook lemma interfaces,
   and honest complexity accounting for both list and heap realizations; test
   and commit.
4. Add the empty-start caching theorem/model boundary, test, and commit.
5. Update the audit, textbook map, progress totals, Chapter 15 trust surface,
   and site navigation; run focused and root verification, then integrate into
   `main`, push, and close issue #317 with evidence.
