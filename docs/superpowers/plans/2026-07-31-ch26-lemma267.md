# Chapter 26 Lemma 26.7 Implementation Plan

**Goal:** Prove the concrete Edmonds--Karp residual-distance monotonicity
theorem for the actual `Flow.augment` operation, without an assumed
augmentation-edge compatibility certificate.

**Architecture:** Replace the disconnected indexed shortest-path bundle with a
bundle around the existing list-based `Flow.AugmentingPath`.  Put generic
path-list and augmentation-semantics facts in the Ford--Fulkerson support
module; keep residual-distance, prefix optimality, and Lemma 26.7 in the
Edmonds--Karp analysis page.

## Task 1: Lock the bridge interface (RED)

- Add `Tests/Chapter_26_Edmonds_Karp_Interface.lean`.
- Check path edge length/index extraction, residual-path composition,
  predecessor existence, shortest prefixes, and the real augmentation-edge
  bridge.
- Run the file once and record the expected `unknownIdentifier` failure.

## Task 2: Prove the concrete shortest-path foundation (GREEN)

- Add `Flow.ResidualPath.edges_length` and edge-index extraction.
- Add the exact residual-capacity update and the theorem that every new
  residual edge reverses an edge of the augmented path.
- Add `ResidualPathLength.trans` and `IsShortestDist.exists_predecessor`.
- Change `ShortestAugmentingPath` to contain `path : Flow.AugmentingPath φ`
  and its shortest-distance certificate.
- Prove segment, prefix, suffix, and shortest-prefix facts.
- Compile the focused production pages and interface with warnings as errors,
  then commit this foundation without changing the remaining-group count.

## Task 3: Prove Lemma 26.7 (RED -> GREEN)

- Extend the interface with
  `ShortestAugmentingPath.exists_shortestDist_le_augment` and
  `shortest_path_nondec`; first observe RED.
- Lift every finite path in the augmented residual network back to an old
  shortest distance no larger than its path length.  For old edges use the
  triangle lemma; for new edges use the concrete reverse-edge bridge and
  shortest-prefix optimality.
- Derive `shortest_path_nondec` by uniqueness of old shortest distances.
- Add an application-shaped interface example and `#print axioms`.

## Task 4: Close the status group

- Update Chapter 26 prose, status board, proof map, CSV evidence, generated
  progress page, and generated README table.
- Change Chapter 26 from `9/9/3` to `10/10/2`; leave executable BFS/EK and
  matching/Theorem 26.12 as the two remaining groups.
- Run direct warning-as-error checks, `lake build CLRSLean.Chapter_26`, the
  repository consistency gate, generated-file checks, placeholder scan, and
  `git diff --check`.
- Obtain an independent proof review, commit, and fast-forward to `main`.
