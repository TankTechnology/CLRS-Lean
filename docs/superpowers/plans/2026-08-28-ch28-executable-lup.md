# Chapter 28 Executable LUP Implementation Plan

**Goal:** Implement and verify a total, costed LUP decomposition and attach the
quadratic LUP-SOLVE work claim to a costed solver execution.

**Architecture:** Reuse the existing block-factorization lemmas only in proofs.
Production data are generated by explicit pivot scanning, pointwise
elimination, dimension recursion, and block assembly.  Keep every difficult
layer in a separate Lean file.

---

### Task 1: Pin the public interface in RED

**Files:**
- Create: `Tests/Chapter_28_ExecutableLUP_Interface.lean`

- [x] Check `findPivotWithCost`, `eliminateWithCost`,
  `lupDecomposeWithCost`, successful-result soundness, nonsingular success,
  failure/singularity, cubic work, costed LUP-SOLVE erasure, and quadratic work.
- [x] Run the focused file and record that all new names are initially unknown.

### Task 2: Define pivot and elimination executions

**Files:**
- Create: `.../ExecutableLUP/Basic.lean`
- Create: `.../ExecutableLUP/Pivot.lean`
- Create: `.../ExecutableLUP/Elimination.lean`

- [x] Define factor/result records and a list-recursive pivot scan with an
  actual comparison counter.
- [x] Prove found-pivot soundness, failure completeness, and the linear scan
  bound.
- [x] Define a pointwise elimination execution and sum its entry costs.
- [x] Prove erasure to the existing elimination matrix product, column-zeroing,
  and the quadratic local bound.

### Task 3: Define the recursive LUP execution

**Files:**
- Create: `.../ExecutableLUP/Execution.lean`

- [x] Define the computable `Fin (n+1) ≃ Fin 1 ⊕ Fin n` reindexing and the
  parent-factor assembly operations.
- [x] Define `lupDecomposeWithCost` by recursion on dimension, using only the
  explicit pivot and elimination executions.
- [x] Add rational success/failure regression examples.

### Task 4: Prove factorization and failure characterization

**Files:**
- Create: `.../ExecutableLUP/Correctness.lean`

- [x] Prove soundness of every successful result by induction, reusing the
  block algebra of `exists_lup_decomposition`.
- [x] Prove a nonsingular parent has a pivot and a nonsingular Schur child.
- [x] Prove nonsingular success and derive `result = none → det A = 0`.
- [x] Bundle the returned triangular invariants and exact factorization.

### Task 5: Prove execution-derived work

**Files:**
- Create: `.../ExecutableLUP/Cost.lean`

- [x] Derive the recurrence directly from `LUPExecution.work`.
- [x] Prove `work ≤ 4 * n^3` for every success or failure path.
- [x] Publish a nonsingular execution bundle containing both correctness and
  the cubic bound.

### Task 6: Cost LUP-SOLVE

**Files:**
- Create: `.../ExecutableLUP/SolveCost.lean`

- [x] Define costed forward and backward substitution recursions.
- [x] Prove erasure to `forwardSubst`, `backSubst`, and `lupSolve`.
- [x] Prove the same run has quadratic work and inherits `lupSolve_correct`.

### Task 7: Publish and audit

**Files:**
- Create: `.../ExecutableLUP.lean`
- Modify: `CLRSLean/FourthEdition/Chapter_28.lean`
- Modify: `Tests/Trust/Chapter_28.lean`
- Modify: progress ledger, generated dashboard/README, and whole-book audit.

- [x] Import the facade from canonical Chapter 28 and state the exact-algebra
  unit-cost boundary.
- [x] Run every small file, one Chapter 28 build, focused interface/trust,
  global axiom audit, repository checks, and `git diff --check`.
- [x] Commit and push the verified checkpoint; close #340 only after all gates
  pass.
